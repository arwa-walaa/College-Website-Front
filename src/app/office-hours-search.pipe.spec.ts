import { OfficeHoursSearchPipe } from './office-hours-search.pipe';

describe('OfficeHoursSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new OfficeHoursSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
